{
  "name": "shopify-waitlist-app",
  "version": "1.0.0",
  "description": "Back-in-stock waitlist private app for Shopify (ki-tools-2)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:dashboard": "cd dashboard && npm run build",
    "build": "npm run build:dashboard"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "node-fetch": "^2.7.0",
    "resend": "^6.9.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
